T = int(input())

for tc in range(1, T+1):
    K, N, M = list(map(int, input().split()))
    charger_num = list(map(int, input().split()))

    charger = [0]*(N+1)

    while True:
        if charger[i+K] = 1:    # 간 최대치에서 충전소가 있을 때때
            i+    #     if charger[0+K] == 1:   #0에서 최대치 움직였을 때 충전기 있으면
    #         cnt_charge += 1     #충전횟수 +1


            # 만약 충전기 설치가 잘못되어 종점에 도착할 수 없는 경우는 0을 출력한다.
            # index가 N+1-K 안에 충전소가 없으면 0

